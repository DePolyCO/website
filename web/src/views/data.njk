---
permalink: /static/article-data.json
---

[{% for article in articles.all %}{% if loop.index0 > 1 and article._id != featuredArticle._id %}{"title":"{{ article.title | safe }}","date":"{{ article.publishedAt }}","content": "{% trim article.intro,400 %}","url":"{{ article.settings.slug.current | slugify }}","postCategory":"{{ article.settings.postCategory }}","mainImage":"{% imageUrlFor article.mainImage,800 %}"}{% if loop.revindex0 != 1 %},{% endif %}{% endif %}{% endfor %}]